#include "../includes/Gomoku.hpp"

int		Heuristic::captureSearchPatterns(Board &board,
	t_vec2 point, t_Color playerColor, t_dir dir, char *line, char *backLine)
{
	int				ret = 0;

	// TODO: capture search patterns.

	
	return (ret);
}
